#pragma once

#define VERSION_MAJOR @PROJECT_VERSION_MAJOR@
#define VERSION_MINOR @PROJECT_VERSION_MINOR@
#define VERSION_PATCH @PROJECT_VERSION_PATCH@

#cmakedefine GIT_REVISION @GIT_REVISION@

#define __VERSION_QUOTE(x__)         #x__
#define _VERSION_QUOTE(x__)          __VERSION_QUOTE(x__)
#define _VERSION_JOIN(x__, y__, z__) x__.y__.z__

#define VERSION_FULL _VERSION_QUOTE(_VERSION_JOIN(VERSION_MAJOR, VERSION_MINOR, VERSION_PATCH))
